# Vowel or Consonant

This project is a simple C++ program that determines if a given character is a vowel, consonant, or not an alphabet. The program uses the `isalpha` function from the C++ standard library to check if the input is an alphabet.

## Features

- Check if a character is a vowel.
- Check if a character is a consonant.
- Identify if the character is not an alphabet.

## Getting Started

### Prerequisites

- A C++ compiler (e.g., GCC)

### Installation

1. Clone the repository or download the source code.
2. Navigate to the project directory.

### Usage

1. Compile the code using the following command:
    ```sh
    g++ -o VowelOrConsonant VowelOrConsonant.cpp
    ```
2. Run the executable:
    ```sh
    ./VowelOrConsonant
    ```


